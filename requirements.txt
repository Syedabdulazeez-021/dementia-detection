# IMPORTANT: Use 64-bit Python 3.10, 3.11, or 3.12 (NOT 3.13).
# mediapipe is pinned to 0.10.21 because newer releases (>=0.10.31) REMOVED the
# legacy `mediapipe.solutions` API (mp.solutions.face_mesh) that this project uses,
# and versions that still have it do not ship wheels for Python 3.13.
tensorflow>=2.13.0
opencv-python>=4.8.0
mediapipe==0.10.21
numpy>=1.24.0
scipy>=1.9.0
scikit-learn>=1.2.0
joblib>=1.2.0
scikit-image>=0.21.0
flask>=2.3.0
flask-cors>=4.0.0
pandas>=2.0.0
matplotlib>=3.7.0
Pillow>=10.0.0
librosa>=0.10.0
sounddevice>=0.4.6
openpyxl>=3.1.0
python-dateutil>=2.8.0